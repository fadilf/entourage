# Nexus Roadmap

## Short-term
- [ ] Render agent messages with Markdown formatting (headings, code blocks, lists, bold/italic, etc.)
- [ ] Syntax highlighting in code blocks
- [ ] Fix/support @mentioning a different agent than the thread's current agent (may be a bug)
- [ ] Editable thread names (rename threads after creation)
- [ ] Direct reply to specific messages

## Medium-term
- [ ] File attachment support (send files/images as context to agents)
- [ ] Orchestrator agent — a top-layer agent that coordinates between other agents in a thread

## Long-term
